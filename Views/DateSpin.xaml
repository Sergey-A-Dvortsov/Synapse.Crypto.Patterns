<UserControl x:Class="Synapse.Crypto.Patterns.DateSpin"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:local="clr-namespace:Synapse.Crypto.Patterns"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="200">
    <UserControl.Resources>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="1"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="DarkBlue"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>

    </UserControl.Resources>
    <Grid>
        <Border BorderBrush="DarkGray" BorderThickness="1" CornerRadius="2" Margin="2"
            HorizontalAlignment="Stretch" VerticalAlignment="Stretch" >
        <Grid Height="Auto">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <xctk:IconButton Command="{Binding MinusTimeCommand}">
                <xctk:IconButton.Icon>
                    <Image Source="/Resources/left_icon_32.png" />
                </xctk:IconButton.Icon>
            </xctk:IconButton>
            <Border Margin="1" BorderThickness="0.1" BorderBrush="LightGray" Height="Auto">
                <TextBlock Grid.Column="1" Text="{Binding Time, StringFormat={}{0: dd.MM.yyyy}}" />
            </Border>
            <xctk:IconButton Grid.Column="2" Command="{Binding PlusTimeCommand}">
                <xctk:IconButton.Icon>
                    <Image Source="/Resources/right_icon_32.png" />
                </xctk:IconButton.Icon>
            </xctk:IconButton>
        </Grid>
    </Border>
    </Grid>
</UserControl>
