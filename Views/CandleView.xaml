<UserControl x:Class="Synapse.Crypto.Patterns.CandleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Synapse.Crypto.Patterns"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="200">
    <UserControl.Resources>
        
        <Style x:Key="HeaderCandleStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="Gray"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="ValueCandleStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="Blue"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>
    <Border BorderThickness="1" BorderBrush="LightGray" CornerRadius="2">
        <Grid Margin="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="{Binding Candle.OpenTime, StringFormat={}{0:dd.MM.yy HH:mm}}" Style="{StaticResource ValueCandleStyle}"/>
            <TextBlock Grid.Column="1" Text="Open" Style="{StaticResource HeaderCandleStyle}"/>
            <TextBlock Grid.Column="2" Text="{Binding Candle.Open}" Style="{StaticResource ValueCandleStyle}"/>
            <TextBlock Grid.Column="3" Text="High" Style="{StaticResource HeaderCandleStyle}"/>
            <TextBlock Grid.Column="4" Text="{Binding Candle.High}" Style="{StaticResource ValueCandleStyle}"/>
            <TextBlock Grid.Column="5" Text="Low" Style="{StaticResource HeaderCandleStyle}"/>
            <TextBlock Grid.Column="6" Text="{Binding Candle.Low}" Style="{StaticResource ValueCandleStyle}"/>
            <TextBlock Grid.Column="7" Text="Close" Style="{StaticResource HeaderCandleStyle}"/>
            <TextBlock Grid.Column="8" Text="{Binding Candle.Close}" Style="{StaticResource ValueCandleStyle}"/>
        </Grid>
    </Border>
</UserControl>
